{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "paste-image",
  "description": "Lets service tabs fetch a cross-origin image for clipboard paste. Copying an image in a browser puts only a URL on the clipboard, and image CDNs commonly serve no Access-Control-Allow-Origin header, so the page itself cannot read the bytes. This grants ONE narrowly-scoped command and nothing else - deliberately not core:default, so remote pages get no other IPC surface.",
  "windows": ["main"],
  "webviews": ["linkedin", "whatsapp", "messenger", "bluesky"],
  "remote": {
    "urls": [
      "https://*.linkedin.com",
      "https://web.whatsapp.com",
      "https://*.messenger.com",
      "https://bsky.app"
    ]
  },
  "permissions": ["allow-fetch-image-as-data-uri"]
}
